# 🚀 QUICK START GUIDE

## Первый запуск после рефакторинга

### 1. Установите зависимости

```bash
npm install
```

Это установит все новые пакеты:

- `react-router-dom` - маршрутизация
- `zustand` - state management  
- `react-helmet-async` - SEO
- `@supabase/supabase-js` - backend (опционально)

### 2. Запустите приложение

```bash
npm run dev
```

### 3. Откройте в браузере

```
http://localhost:3000
```

---

## ✅ Что уже работает

✔️ **Маршрутизация** - URL адреса для каждой страницы  
✔️ **State Management** - Централизованное состояние через Zustand  
✔️ **Плеер UI** - Полный интерфейс с seek, volume, repeat, shuffle  
✔️ **Навигация** - Клик по треку устанавливает плейлист  
✔️ **Аутентификация** - Mock login (нужна Web3 интеграция)  
✔️ **SEO** - Динамические мета-теги  

---

## ⚠️ Что нужно добавить

### Реальные аудио-файлы

**Проблема:** Плеер готов, но треки не имеют `audioUrl`

**Решение:**

1. Добавьте MP3 файлы в `public/music/` или загрузите на CDN
2. Обновите `services/mockData.ts`:

```typescript
export const MOCK_TRACKS: Track[] = [
  {
    id: '1',
    title: "Cosmic Dreams",
    artist: "DJ_Void",
    duration: "3:45",
    coverUrl: "https://picsum.photos/300/300?random=1",
    audioUrl: "/music/cosmic-dreams.mp3", // ← Добавьте это!
    plays: 142350,
    likes: 8932,
    isNft: true
  },
  // ...
];
```

---

## 🎯 Тестирование

### Проверьте навигацию

1. Кликните на разные пункты меню
2. URL должен меняться (`/feed`, `/trends`, и т.д.)
3. Кнопка назад/вперед браузера должна работать

### Проверьте плеер

1. Кликните на трек в Feed
2. Плеер появится внизу
3. Попробуйте Play/Pause, Next, Previous
4. Потяните seek bar и volume slider
5. Переключите repeat и shuffle

### Проверьте state

1. Кликните на трек
2. Откройте DevTools → React DevTools
3. Проверьте Zustand stores (components использующие hooks)

---

## 🔧 Troubleshooting

### "Cannot find module" ошибки

**Решение:**

```bash
rm -rf node_modules
rm package-lock.json
npm install
```

### Плеер не показывается

**Причина:** Нужно кликнуть на трек для воспроизведения

**Проверка:**

```typescript
// В DevTools Console
usePlayerStore.getState().currentTrack // Должен быть Track object
```

### Routing не работает

**Проверка:** Убедитесь что `index.html` загружается с `/` а не `file://`

---

## 📝 Следующие шаги

1. ✅ **Запустите** - `npm install && npm run dev`
2. ⏳ **Добавьте аудио** - Загрузите MP3 файлы
3. ⏳ **Интегрируйте Supabase** - Создайте проект на supabase.com
4. ⏳ **Web3 Auth** - Подключите MetaMask/Phantom

---

## 📚 Документация

- **Full README**: `README_REFACTOR.md`
- **Stores**: `stores/` - Zustand state management
- **Routes**: `routes/AppRoutes.tsx` - Router configuration
- **Player**: `components/PlayerBar.tsx` - Audio player UI

---

**Все готово! Запускайте и тестируйте** 🎉
